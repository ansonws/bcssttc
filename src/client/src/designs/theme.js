import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#364976',
    },
    secondary: {
      main: '#FFC857',
    },
    error: {
      main: '#EC2C3E',
    }
  },
});

export default theme;
